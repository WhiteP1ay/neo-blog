---
description: 客户端组件结构：单一职责、拆分、hooks 与 UI 分离、复用 shadcn/ui、删除死代码
globs: 
alwaysApply: false
---

# 组件与模块设计（neo-blog）

在编写或重构react组件时遵循：

## 职责与文件

- **单一职责**：一个文件只做一件事；UI层只组合子组件与传参，不写大块业务 JSX。
- **组件化**：单文件过长时拆成子组件（按区域/面板/弹层拆分），避免「上帝组件」。
- **死代码**：不维护的场景直接删文件与引用，勿留注释掉的整块逻辑。

## 逻辑与 UI

- **hooks 化**：副作用、路由、服务端 action 调用、表单/弹窗状态、本地存储等放在同目录的 `useXxx.ts`（或 `use-xxx.ts`），**不要**与展示 JSX 混在同一组件里堆 `useCallback`。
- 展示组件尽量 **无业务闭包**：通过 props 接收数据与回调。

## UI 复用

- 优先使用 **`/components/ui`** 与 **shadcn** 已有组件（Button、Dialog、Dropdown 等），避免复制一套相同样式的原生控件。
- 多处相同的菜单项 / 行布局抽成小组件或小型工厂，**禁止**维护两套几乎相同的 `ContextMenu` 与 `DropdownMenu` JSX。

## 反例 / 正例

```tsx
// ❌ BAD：单文件内上千行 + 业务与 JSX 交织
export function Screen() {
  const onSave = useCallback(async () => { await updateThing(); ... }, [...]);
  return <div>... 500 lines ...</div>;
}

// ✅ GOOD：编排 + hooks + 子组件
export function Screen() {
  const nav = useScreenNav();
  const actions = useScreenActions(nav.refresh);
  return (
    <>
      <ScreenHeader onSettings={actions.openSettings} />
      <ScreenBody {...actions} />
    </>
  );
}
```
