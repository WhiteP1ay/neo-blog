---
description: 前台/UI 改动后的验收：Biome、Playwright CLI 或可重复 E2E，并在关键路径补测试以便重构
globs:
  - "app/**/*"
  - "components/**/*"
alwaysApply: false
---

# 前端改动验收与回归测试（neo-blog）

在完成**涉及页面、路由、布局、全局交互或可访问行为**的代码修改后，不要仅凭静态阅读下结论；用可重复的方式做最小验收。

## 何时必须验收

适用于本规则匹配路径下的功能性改动（不仅是样式微调），例如：

- 导航、表单、对话框、快捷键、列表/分页、国际化路径（`/`、`/en/`）
- `middleware`、`next.config` 中与前台导航相关的行为（重定向等）

若是纯文案标点、注释、无行为影响的类型收紧，可选用更轻的检查（如下仅跑 Biome）。

## 收尾步骤（按需组合，从小到大）

1. **Lint / 格式**：对改动文件运行 `pnpm exec biome check --write <路径>`，或整个项目的 `pnpm run lint`（以仓库脚本为准）。
2. **浏览器证据（二选一或都做）**
   - **Playwright CLI**：对关键流程做真实浏览器复核（快照、点击、键盘）。适用于探索式验收或 CLI 可用的环境；参考全局 **`playwright-cli` Skill**。
   - **仓库 E2E**：运行 `pnpm test:e2e`（必要时加 `--grep` 缩小范围）。适用于已有用例或可快速写成 spec 的路径。
3. **构建（可选）**：路由删除、`next.config`、大范围重构后，运行 `pnpm run build` 确认类型与构建通过。

验收原则与子代理 **[`.cursor/agents/verifier.md`](.cursor/agents/verifier.md)** 一致：**用命令输出与浏览器行为作为证据**，并写明尚未覆盖的场景。

## 何时写或更新 `e2e/` 用例

在下列情况应**新增或调整** Playwright spec（宁可小而稳，不要大而脆）：

- 新增用户可见流程（登录外流程、搜索、提交、权限差异）
- 曾出过 bug 的交互（回归）
- 依赖快捷键、焦点陷阱、滚动容器内点击等「静态代码难看穿」的行为

命名与断言优先使用 **role / 文案 / 稳定的数据属性**，避免仅依赖易变的 CSS class。

## 与「只跑 build」的分工

- **build**：抓类型与编译、路由完整性。
- **Playwright / E2E**：抓 DOM、焦点、 hydration、服务端与客户端边界上的行为。

两者互补；涉及交互的改动**不应**只靠 build 宣称完备。
