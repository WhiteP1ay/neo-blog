#
DATABASE_URL=

# 连接池参数（可选）
DB_POOL_MAX=5
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECT_TIMEOUT_MS=10000

# SSL 策略（可选）
# 生产环境默认开启 SSL；本地开发通常无需设置
# DB_SSL=true
# DB_SSL_DISABLE=true

# 初始化管理员账户（执行 pnpm db:seed-admin 时读取）
ADMIN_NAME=admin
ADMIN_PASSWORD=change_me_to_a_strong_password
